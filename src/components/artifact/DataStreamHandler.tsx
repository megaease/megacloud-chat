// components/artifact/DataStreamHandler.tsx
"use client";

interface DataStreamHandlerProps {
  chatId: string;
}

export function DataStreamHandler({ chatId }: DataStreamHandlerProps) {
  // TODO: Update for AI SDK 5 - data stream handling has changed
  console.log(
    "DataStreamHandler temporarily disabled for AI SDK 5 migration, chatId:",
    chatId
  );
  return null;
}
